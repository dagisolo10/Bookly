import type { Metadata } from "next";

export const defaultMetadata: Metadata = {
    title: "Bookly | Appointment Booking",
    description: "Book appointments with trusted local businesses in minutes.",
};
